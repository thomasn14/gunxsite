---
// This skip link allows users using a keyboard or assistive technology to skip directly to the main content of the page.
// It is visually hidden off-screen by default, but becomes visible when focused.

---

<a href="#main-content" class="skip-link">Skip to main content</a>
<style>
  .skip-link {
    position: absolute;
    top: -100rem;
    left: 1rem;
    background: var(--color-accent);
    color: var(--color-background);
    padding: 8px;
    z-index: 100;
    text-decoration: none;
    font-family: sans-serif;
    font-weight: bold;
  }

  .skip-link:focus {
    top: 1rem;
  }
</style>
